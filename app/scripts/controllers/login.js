'use strict';

/**
 * @ngdoc function
 * @name applicationApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Controller of the applicationApp
 */
angular.module('applicationApp')
  .controller('LoginCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
